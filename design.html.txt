<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Aether — Minecraft Launcher</title>
<link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@300;400;600;700;900&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --glass-bg: rgba(255,255,255,0.04);
    --glass-border: rgba(255,255,255,0.10);
    --glass-shine: rgba(255,255,255,0.07);
    --blur: blur(24px);
    --green: #4ade80;
    --green-dim: #22c55e;
    --emerald: #10b981;
    --sky: #38bdf8;
    --amber: #fbbf24;
    --red: #f87171;
    --text: #f0fdf4;
    --text-muted: rgba(240,253,244,0.45);
    --text-dim: rgba(240,253,244,0.25);
    --accent: #4ade80;
    --shadow-glow: 0 0 60px rgba(74,222,128,0.12);
  }

  html, body {
    width: 100%; height: 100%; overflow: hidden;
    font-family: 'DM Sans', sans-serif;
    background: #060d0a;
    color: var(--text);
  }

  /* ─── BACKGROUND ─── */
  .bg {
    position: fixed; inset: 0; z-index: 0;
    background: 
      radial-gradient(ellipse 80% 60% at 20% 10%, rgba(16,185,129,0.18) 0%, transparent 60%),
      radial-gradient(ellipse 60% 50% at 80% 80%, rgba(56,189,248,0.10) 0%, transparent 55%),
      radial-gradient(ellipse 50% 40% at 50% 50%, rgba(74,222,128,0.06) 0%, transparent 60%),
      #060d0a;
  }

  .bg-grid {
    position: fixed; inset: 0; z-index: 0;
    background-image:
      linear-gradient(rgba(74,222,128,0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(74,222,128,0.04) 1px, transparent 1px);
    background-size: 48px 48px;
    mask-image: radial-gradient(ellipse 90% 90% at 50% 50%, black 30%, transparent 80%);
  }

  /* floating orbs */
  .orb {
    position: fixed; border-radius: 50%; pointer-events: none; z-index: 0;
    filter: blur(80px); animation: orbFloat 12s ease-in-out infinite;
  }
  .orb-1 { width: 500px; height: 500px; background: rgba(16,185,129,0.12); top: -120px; left: -100px; animation-delay: 0s; }
  .orb-2 { width: 400px; height: 400px; background: rgba(56,189,248,0.08); bottom: -80px; right: -60px; animation-delay: -5s; }
  .orb-3 { width: 300px; height: 300px; background: rgba(74,222,128,0.07); top: 40%; left: 60%; animation-delay: -9s; }

  @keyframes orbFloat {
    0%, 100% { transform: translate(0,0) scale(1); }
    33% { transform: translate(30px, -20px) scale(1.05); }
    66% { transform: translate(-20px, 15px) scale(0.97); }
  }

  /* ─── LAYOUT ─── */
  .launcher {
    position: relative; z-index: 1;
    width: 100vw; height: 100vh;
    display: grid;
    grid-template-columns: 260px 1fr;
    grid-template-rows: 1fr;
    gap: 0;
    overflow: hidden;
  }

  /* ─── SIDEBAR ─── */
  .sidebar {
    display: flex; flex-direction: column;
    padding: 28px 20px;
    background: rgba(6,13,10,0.55);
    border-right: 1px solid var(--glass-border);
    backdrop-filter: blur(40px);
    gap: 6px;
    animation: slideInLeft 0.6s cubic-bezier(0.16,1,0.3,1) both;
  }
  @keyframes slideInLeft {
    from { transform: translateX(-30px); opacity: 0; }
    to   { transform: translateX(0);     opacity: 1; }
  }

  .logo {
    display: flex; align-items: center; gap: 12px;
    padding: 12px 14px 24px;
    border-bottom: 1px solid var(--glass-border);
    margin-bottom: 10px;
  }

  .logo-cube {
    width: 36px; height: 36px;
    position: relative; flex-shrink: 0;
  }

  .cube-face {
    position: absolute;
    border-radius: 5px;
  }
  .cube-top    { width: 36px; height: 36px; background: linear-gradient(135deg, #4ade80, #22c55e); top: 0; left: 0; clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%); box-shadow: 0 0 20px rgba(74,222,128,0.5); }

  .logo-text {
    font-family: 'Unbounded', sans-serif;
    font-size: 18px; font-weight: 700;
    letter-spacing: -0.5px;
    background: linear-gradient(135deg, #4ade80, #a7f3d0);
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  }
  .logo-sub {
    font-size: 9px; font-weight: 300; letter-spacing: 3px;
    color: var(--text-dim); text-transform: uppercase;
    font-family: 'DM Sans', sans-serif;
    -webkit-text-fill-color: initial;
  }

  .nav-label {
    font-size: 9px; font-weight: 500; letter-spacing: 2.5px;
    color: var(--text-dim); text-transform: uppercase;
    padding: 14px 14px 6px;
  }

  .nav-item {
    display: flex; align-items: center; gap: 12px;
    padding: 11px 14px; border-radius: 12px; cursor: pointer;
    transition: all 0.2s ease;
    font-size: 13.5px; font-weight: 400;
    color: var(--text-muted);
    position: relative; overflow: hidden;
    border: 1px solid transparent;
    user-select: none;
  }
  .nav-item:hover {
    background: var(--glass-bg);
    color: var(--text);
    border-color: var(--glass-border);
  }
  .nav-item.active {
    background: rgba(74,222,128,0.08);
    color: var(--green);
    border-color: rgba(74,222,128,0.18);
    font-weight: 500;
  }
  .nav-item.active::before {
    content: '';
    position: absolute; left: 0; top: 20%; bottom: 20%;
    width: 3px; border-radius: 0 3px 3px 0;
    background: var(--green);
    box-shadow: 0 0 10px var(--green);
  }
  .nav-icon {
    width: 18px; height: 18px; flex-shrink: 0;
    display: flex; align-items: center; justify-content: center;
    opacity: 0.7;
  }
  .nav-item.active .nav-icon { opacity: 1; }

  .sidebar-bottom {
    margin-top: auto;
    padding-top: 16px;
    border-top: 1px solid var(--glass-border);
  }

  .user-card {
    display: flex; align-items: center; gap: 12px;
    padding: 12px 14px; border-radius: 12px;
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    cursor: pointer;
    transition: all 0.2s;
  }
  .user-card:hover { border-color: rgba(74,222,128,0.25); }

  .avatar {
    width: 36px; height: 36px; border-radius: 8px;
    background: linear-gradient(135deg, #064e3b, #065f46);
    display: grid; grid-template-columns: 1fr 1fr;
    gap: 2px; padding: 5px;
    border: 1px solid rgba(74,222,128,0.3);
    flex-shrink: 0;
  }
  .avatar-pixel {
    border-radius: 2px;
  }
  .px-a { background: #4ade80; }
  .px-b { background: #166534; }
  .px-c { background: #22c55e; }
  .px-d { background: #15803d; }

  .user-info { flex: 1; min-width: 0; }
  .user-name { font-size: 13px; font-weight: 500; color: var(--text); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .user-status { font-size: 11px; color: var(--green); display: flex; align-items: center; gap: 5px; }
  .status-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--green); box-shadow: 0 0 6px var(--green); animation: pulse 2s ease-in-out infinite; }
  @keyframes pulse { 0%,100%{ opacity:1; } 50%{ opacity:0.4; } }

  /* ─── MAIN ─── */
  .main {
    display: flex; flex-direction: column;
    overflow: hidden;
    animation: fadeIn 0.7s cubic-bezier(0.16,1,0.3,1) 0.1s both;
  }
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* ─── TOP BAR ─── */
  .topbar {
    display: flex; align-items: center; gap: 16px;
    padding: 22px 32px 0;
    flex-shrink: 0;
  }
  .topbar-title {
    font-family: 'Unbounded', sans-serif;
    font-size: 20px; font-weight: 600;
    letter-spacing: -0.5px;
  }
  .topbar-sub {
    font-size: 12px; color: var(--text-muted); font-weight: 300;
  }
  .topbar-right { margin-left: auto; display: flex; align-items: center; gap: 12px; }

  .search-box {
    display: flex; align-items: center; gap: 10px;
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    border-radius: 12px; padding: 9px 16px;
    backdrop-filter: var(--blur);
    transition: all 0.2s;
    width: 220px;
  }
  .search-box:focus-within {
    border-color: rgba(74,222,128,0.35);
    box-shadow: 0 0 0 3px rgba(74,222,128,0.06);
  }
  .search-box input {
    background: none; border: none; outline: none;
    font-family: 'DM Sans', sans-serif;
    font-size: 13px; color: var(--text); width: 100%;
  }
  .search-box input::placeholder { color: var(--text-dim); }

  .btn-icon {
    width: 38px; height: 38px; border-radius: 10px;
    background: var(--glass-bg); border: 1px solid var(--glass-border);
    display: flex; align-items: center; justify-content: center;
    cursor: pointer; transition: all 0.2s; color: var(--text-muted);
  }
  .btn-icon:hover { border-color: rgba(74,222,128,0.3); color: var(--green); }

  /* ─── CONTENT ─── */
  .content {
    flex: 1; overflow-y: auto; overflow-x: hidden;
    padding: 24px 32px 32px;
    display: flex; flex-direction: column; gap: 24px;
  }
  .content::-webkit-scrollbar { width: 4px; }
  .content::-webkit-scrollbar-track { background: transparent; }
  .content::-webkit-scrollbar-thumb { background: rgba(74,222,128,0.2); border-radius: 2px; }

  /* ─── HERO CARD ─── */
  .hero-card {
    position: relative; border-radius: 20px; overflow: hidden;
    height: 200px; flex-shrink: 0;
    border: 1px solid var(--glass-border);
    cursor: pointer;
  }
  .hero-bg {
    position: absolute; inset: 0;
    background: 
      linear-gradient(135deg, rgba(6,78,59,0.9) 0%, rgba(6,13,10,0.95) 60%),
      repeating-linear-gradient(
        0deg, transparent, transparent 32px,
        rgba(74,222,128,0.04) 32px, rgba(74,222,128,0.04) 33px
      ),
      repeating-linear-gradient(
        90deg, transparent, transparent 32px,
        rgba(74,222,128,0.04) 32px, rgba(74,222,128,0.04) 33px
      );
  }

  /* Minecraft pixel scene */
  .pixel-scene {
    position: absolute; right: 0; top: 0; bottom: 0;
    width: 50%;
    display: flex; align-items: flex-end; justify-content: flex-end;
    padding: 0 30px 0 0;
    overflow: hidden;
  }

  .block-grid {
    display: grid; gap: 3px;
    grid-template-columns: repeat(8, 22px);
    grid-template-rows: repeat(5, 22px);
    transform: perspective(400px) rotateX(10deg) rotateY(-8deg);
    filter: drop-shadow(0 20px 40px rgba(0,0,0,0.5));
    animation: blockFloat 4s ease-in-out infinite;
  }
  @keyframes blockFloat {
    0%,100% { transform: perspective(400px) rotateX(10deg) rotateY(-8deg) translateY(0); }
    50%      { transform: perspective(400px) rotateX(10deg) rotateY(-8deg) translateY(-6px); }
  }

  .blk {
    border-radius: 3px;
    position: relative;
    box-shadow: inset -3px -3px 0 rgba(0,0,0,0.25), inset 2px 2px 0 rgba(255,255,255,0.12);
  }
  .blk-g  { background: linear-gradient(135deg, #4ade80, #16a34a); }
  .blk-d  { background: linear-gradient(135deg, #713f12, #451a03); }
  .blk-s  { background: linear-gradient(135deg, #6b7280, #374151); }
  .blk-w  { background: linear-gradient(135deg, #93c5fd, #3b82f6); box-shadow: inset -3px -3px 0 rgba(0,0,0,0.2), 0 0 8px rgba(147,197,253,0.3); }
  .blk-e  { background: linear-gradient(135deg, #a78bfa, #7c3aed); box-shadow: inset -3px -3px 0 rgba(0,0,0,0.25), 0 0 10px rgba(167,139,250,0.4); animation: glowPulse 2s ease-in-out infinite; }
  .blk-x  { background: transparent; }
  @keyframes glowPulse { 0%,100%{ opacity:1; } 50%{ opacity:0.6; } }

  .hero-content {
    position: absolute; inset: 0;
    padding: 30px 32px;
    display: flex; flex-direction: column; justify-content: space-between;
  }

  .hero-badge {
    display: inline-flex; align-items: center; gap: 6px;
    background: rgba(74,222,128,0.12); border: 1px solid rgba(74,222,128,0.25);
    border-radius: 20px; padding: 5px 12px;
    font-size: 11px; font-weight: 500; color: var(--green);
    letter-spacing: 0.5px; width: fit-content;
  }
  .badge-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--green); animation: pulse 2s infinite; }

  .hero-title {
    font-family: 'Unbounded', sans-serif;
    font-size: 26px; font-weight: 700; line-height: 1.1;
    letter-spacing: -1px;
    text-shadow: 0 2px 20px rgba(0,0,0,0.5);
  }
  .hero-title span { color: var(--green); }

  .hero-bottom { display: flex; align-items: center; gap: 16px; }

  .btn-launch {
    display: flex; align-items: center; gap: 10px;
    background: linear-gradient(135deg, #4ade80, #22c55e);
    color: #052e16; font-weight: 700; font-size: 13px;
    border: none; border-radius: 12px; padding: 12px 24px;
    cursor: pointer; transition: all 0.2s;
    font-family: 'DM Sans', sans-serif;
    box-shadow: 0 4px 20px rgba(74,222,128,0.35), 0 0 0 0 rgba(74,222,128,0.4);
    letter-spacing: 0.3px;
  }
  .btn-launch:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(74,222,128,0.5), 0 0 0 4px rgba(74,222,128,0.1);
  }
  .btn-launch:active { transform: translateY(0); }

  .hero-meta { display: flex; gap: 20px; }
  .meta-item { display: flex; flex-direction: column; gap: 2px; }
  .meta-label { font-size: 10px; letter-spacing: 1px; color: var(--text-dim); text-transform: uppercase; }
  .meta-val { font-size: 13px; font-weight: 500; color: var(--text); }

  /* ─── SECTION HEADER ─── */
  .section-header {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 14px;
  }
  .section-title {
    font-family: 'Unbounded', sans-serif;
    font-size: 13px; font-weight: 600;
    letter-spacing: -0.3px;
  }
  .section-link {
    font-size: 12px; color: var(--green); cursor: pointer;
    opacity: 0.7; transition: opacity 0.2s;
    display: flex; align-items: center; gap: 4px;
  }
  .section-link:hover { opacity: 1; }

  /* ─── VERSION CARDS ─── */
  .versions-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: 12px;
  }

  .version-card {
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    border-radius: 16px; padding: 18px;
    cursor: pointer; transition: all 0.25s ease;
    position: relative; overflow: hidden;
    backdrop-filter: blur(12px);
    animation: cardIn 0.5s cubic-bezier(0.16,1,0.3,1) both;
  }
  .version-card:nth-child(1) { animation-delay: 0.05s; }
  .version-card:nth-child(2) { animation-delay: 0.10s; }
  .version-card:nth-child(3) { animation-delay: 0.15s; }
  .version-card:nth-child(4) { animation-delay: 0.20s; }
  .version-card:nth-child(5) { animation-delay: 0.25s; }

  @keyframes cardIn {
    from { opacity: 0; transform: translateY(16px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .version-card::before {
    content: '';
    position: absolute; inset: 0;
    background: linear-gradient(135deg, var(--glass-shine), transparent);
    pointer-events: none;
  }
  .version-card:hover {
    border-color: rgba(74,222,128,0.3);
    transform: translateY(-3px);
    box-shadow: 0 12px 30px rgba(0,0,0,0.3), var(--shadow-glow);
    background: rgba(74,222,128,0.05);
  }
  .version-card.selected {
    border-color: rgba(74,222,128,0.5);
    background: rgba(74,222,128,0.07);
    box-shadow: 0 0 0 1px rgba(74,222,128,0.2), var(--shadow-glow);
  }

  .version-icon {
    width: 42px; height: 42px; border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    margin-bottom: 14px; font-size: 20px;
    position: relative;
  }
  .vi-vanilla { background: linear-gradient(135deg, rgba(74,222,128,0.2), rgba(34,197,94,0.1)); border: 1px solid rgba(74,222,128,0.25); }
  .vi-fabric  { background: linear-gradient(135deg, rgba(251,191,36,0.2), rgba(245,158,11,0.1)); border: 1px solid rgba(251,191,36,0.25); }
  .vi-forge   { background: linear-gradient(135deg, rgba(248,113,113,0.2), rgba(239,68,68,0.1)); border: 1px solid rgba(248,113,113,0.25); }
  .vi-paper   { background: linear-gradient(135deg, rgba(56,189,248,0.2), rgba(14,165,233,0.1)); border: 1px solid rgba(56,189,248,0.25); }
  .vi-custom  { background: linear-gradient(135deg, rgba(167,139,250,0.2), rgba(139,92,246,0.1)); border: 1px solid rgba(167,139,250,0.25); }

  .version-name { font-size: 13px; font-weight: 600; margin-bottom: 3px; }
  .version-num  { font-size: 11px; color: var(--text-dim); margin-bottom: 12px; }

  .version-tag {
    display: inline-flex; align-items: center;
    font-size: 10px; letter-spacing: 0.5px; font-weight: 500;
    padding: 3px 8px; border-radius: 20px;
  }
  .tag-release { background: rgba(74,222,128,0.1); color: var(--green); border: 1px solid rgba(74,222,128,0.2); }
  .tag-snapshot{ background: rgba(251,191,36,0.1); color: var(--amber); border: 1px solid rgba(251,191,36,0.2); }
  .tag-modded  { background: rgba(248,113,113,0.1); color: var(--red);   border: 1px solid rgba(248,113,113,0.2); }
  .tag-beta    { background: rgba(56,189,248,0.1);  color: var(--sky);   border: 1px solid rgba(56,189,248,0.2); }
  .tag-custom  { background: rgba(167,139,250,0.1); color: #a78bfa;      border: 1px solid rgba(167,139,250,0.2); }

  /* ─── BOTTOM ROW ─── */
  .bottom-row {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 16px;
  }

  /* news feed */
  .news-list { display: flex; flex-direction: column; gap: 10px; }
  .news-item {
    display: flex; align-items: center; gap: 14px;
    background: var(--glass-bg); border: 1px solid var(--glass-border);
    border-radius: 14px; padding: 14px 16px;
    cursor: pointer; transition: all 0.2s;
    backdrop-filter: blur(12px);
  }
  .news-item:hover { border-color: rgba(74,222,128,0.25); transform: translateX(3px); }

  .news-thumb {
    width: 48px; height: 48px; border-radius: 10px; flex-shrink: 0;
    display: flex; align-items: center; justify-content: center;
    font-size: 22px;
  }
  .nt-1 { background: linear-gradient(135deg, #064e3b, #065f46); }
  .nt-2 { background: linear-gradient(135deg, #1e1b4b, #312e81); }
  .nt-3 { background: linear-gradient(135deg, #431407, #7c2d12); }

  .news-content { flex: 1; min-width: 0; }
  .news-title { font-size: 13px; font-weight: 500; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .news-date  { font-size: 11px; color: var(--text-dim); margin-top: 3px; }

  .news-arrow { color: var(--text-dim); flex-shrink: 0; transition: transform 0.2s; }
  .news-item:hover .news-arrow { transform: translateX(3px); color: var(--green); }

  /* stats panel */
  .stats-panel {
    background: var(--glass-bg); border: 1px solid var(--glass-border);
    border-radius: 16px; padding: 20px;
    backdrop-filter: blur(12px);
    display: flex; flex-direction: column; gap: 16px;
  }

  .stat-row { display: flex; flex-direction: column; gap: 8px; }
  .stat-header { display: flex; justify-content: space-between; align-items: center; }
  .stat-name { font-size: 12px; color: var(--text-muted); }
  .stat-value { font-size: 12px; font-weight: 600; }

  .stat-bar {
    height: 5px; border-radius: 3px;
    background: rgba(255,255,255,0.06); overflow: hidden;
  }
  .stat-fill {
    height: 100%; border-radius: 3px;
    animation: fillGrow 1s cubic-bezier(0.16,1,0.3,1) 0.3s both;
  }
  @keyframes fillGrow { from { width: 0 !important; } }
  .sf-green  { background: linear-gradient(90deg, #4ade80, #22c55e); box-shadow: 0 0 6px rgba(74,222,128,0.4); }
  .sf-sky    { background: linear-gradient(90deg, #38bdf8, #0ea5e9); box-shadow: 0 0 6px rgba(56,189,248,0.4); }
  .sf-amber  { background: linear-gradient(90deg, #fbbf24, #f59e0b); box-shadow: 0 0 6px rgba(251,191,36,0.4); }

  .stat-divider { height: 1px; background: var(--glass-border); }

  .playtime-display {
    text-align: center; padding: 8px 0;
  }
  .playtime-num {
    font-family: 'Unbounded', sans-serif;
    font-size: 28px; font-weight: 700;
    background: linear-gradient(135deg, #4ade80, #a7f3d0);
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    line-height: 1;
  }
  .playtime-label { font-size: 10px; color: var(--text-dim); letter-spacing: 2px; text-transform: uppercase; margin-top: 4px; }

  /* ─── LAUNCH BAR ─── */
  .launch-bar {
    display: flex; align-items: center; gap: 16px;
    padding: 16px 32px;
    background: rgba(6,13,10,0.7);
    border-top: 1px solid var(--glass-border);
    backdrop-filter: blur(40px);
    flex-shrink: 0;
  }

  .launch-version-select {
    display: flex; align-items: center; gap: 10px;
    background: var(--glass-bg); border: 1px solid var(--glass-border);
    border-radius: 12px; padding: 10px 16px;
    cursor: pointer; transition: all 0.2s;
  }
  .launch-version-select:hover { border-color: rgba(74,222,128,0.3); }
  .lvs-icon { font-size: 18px; }
  .lvs-text { display: flex; flex-direction: column; }
  .lvs-name { font-size: 13px; font-weight: 500; line-height: 1.2; }
  .lvs-num  { font-size: 11px; color: var(--text-dim); }
  .lvs-arrow { color: var(--text-dim); margin-left: 8px; }

  .launch-spacer { flex: 1; }

  .ram-control {
    display: flex; align-items: center; gap: 12px;
    font-size: 12px; color: var(--text-muted);
  }
  .ram-label { display: flex; flex-direction: column; gap: 1px; }
  .ram-val { font-size: 13px; font-weight: 600; color: var(--text); }
  .ram-sub { font-size: 10px; color: var(--text-dim); }

  .ram-slider {
    -webkit-appearance: none; width: 100px; height: 4px;
    border-radius: 2px; background: rgba(255,255,255,0.08);
    outline: none; cursor: pointer;
  }
  .ram-slider::-webkit-slider-thumb {
    -webkit-appearance: none;
    width: 14px; height: 14px; border-radius: 50%;
    background: var(--green); cursor: pointer;
    box-shadow: 0 0 8px rgba(74,222,128,0.6);
  }

  .btn-launch-main {
    display: flex; align-items: center; gap: 10px;
    background: linear-gradient(135deg, #4ade80, #22c55e);
    color: #052e16; font-weight: 700; font-size: 14px;
    border: none; border-radius: 14px; padding: 13px 32px;
    cursor: pointer; transition: all 0.2s;
    font-family: 'DM Sans', sans-serif;
    letter-spacing: 0.5px;
    box-shadow: 0 4px 24px rgba(74,222,128,0.3), 0 0 0 0 rgba(74,222,128,0);
    position: relative; overflow: hidden;
  }
  .btn-launch-main::after {
    content: '';
    position: absolute; inset: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.15), transparent);
    pointer-events: none;
  }
  .btn-launch-main:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(74,222,128,0.5), 0 0 0 4px rgba(74,222,128,0.1);
  }
  .btn-launch-main:active { transform: translateY(0); }

  /* launch ripple */
  .btn-launch-main.launching {
    animation: launchPulse 0.4s ease-out;
  }
  @keyframes launchPulse {
    0%   { box-shadow: 0 0 0 0 rgba(74,222,128,0.7); }
    100% { box-shadow: 0 0 0 24px rgba(74,222,128,0); }
  }

  /* ─── TOOLTIP / PROGRESS OVERLAY ─── */
  .launch-progress {
    position: fixed; inset: 0; z-index: 100;
    display: flex; align-items: center; justify-content: center;
    background: rgba(6,13,10,0.85);
    backdrop-filter: blur(20px);
    opacity: 0; pointer-events: none;
    transition: opacity 0.3s;
  }
  .launch-progress.visible { opacity: 1; pointer-events: all; }

  .progress-modal {
    background: rgba(10,20,15,0.9);
    border: 1px solid rgba(74,222,128,0.2);
    border-radius: 24px; padding: 40px 48px;
    text-align: center; min-width: 360px;
    box-shadow: 0 40px 80px rgba(0,0,0,0.6), var(--shadow-glow);
  }
  .progress-icon { font-size: 48px; margin-bottom: 16px; animation: spinSlow 3s linear infinite; }
  @keyframes spinSlow { to { transform: rotate(360deg); } }
  .progress-title {
    font-family: 'Unbounded', sans-serif;
    font-size: 18px; font-weight: 600; margin-bottom: 8px;
  }
  .progress-sub { font-size: 13px; color: var(--text-muted); margin-bottom: 28px; }

  .progress-track {
    height: 6px; border-radius: 3px;
    background: rgba(255,255,255,0.06); overflow: hidden; margin-bottom: 12px;
  }
  .progress-fill {
    height: 100%; border-radius: 3px;
    background: linear-gradient(90deg, #4ade80, #22c55e);
    box-shadow: 0 0 10px rgba(74,222,128,0.5);
    width: 0%; transition: width 0.3s ease;
  }
  .progress-pct { font-size: 12px; color: var(--text-dim); }

  /* ─── SVG ICONS inline ─── */
  svg { display: block; }
</style>
</head>
<body>

<div class="bg"></div>
<div class="bg-grid"></div>
<div class="orb orb-1"></div>
<div class="orb orb-2"></div>
<div class="orb orb-3"></div>

<div class="launcher">

  <!-- ─── SIDEBAR ─── -->
  <aside class="sidebar">
    <div class="logo">
      <div class="logo-cube">
        <div class="cube-face cube-top"></div>
      </div>
      <div>
        <div class="logo-text">AETHER</div>
        <div class="logo-sub">Launcher</div>
      </div>
    </div>

    <div class="nav-label">Navigation</div>

    <div class="nav-item active" onclick="setNav(this)">
      <div class="nav-icon">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
      </div>
      Home
    </div>

    <div class="nav-item" onclick="setNav(this)">
      <div class="nav-icon">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
      </div>
      Instances
    </div>

    <div class="nav-item" onclick="setNav(this)">
      <div class="nav-icon">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/></svg>
      </div>
      Mods
    </div>

    <div class="nav-item" onclick="setNav(this)">
      <div class="nav-icon">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14M4.93 4.93a10 10 0 0 0 0 14.14"/></svg>
      </div>
      Servers
    </div>

    <div class="nav-item" onclick="setNav(this)">
      <div class="nav-icon">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="6"/><path d="M15.477 12.89L17 22l-5-3-5 3 1.523-9.11"/></svg>
      </div>
      Achievements
    </div>

    <div class="nav-label">System</div>

    <div class="nav-item" onclick="setNav(this)">
      <div class="nav-icon">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
      </div>
      Settings
    </div>

    <div class="sidebar-bottom">
      <div class="user-card">
        <div class="avatar">
          <div class="avatar-pixel px-a"></div>
          <div class="avatar-pixel px-b"></div>
          <div class="avatar-pixel px-c"></div>
          <div class="avatar-pixel px-d"></div>
        </div>
        <div class="user-info">
          <div class="user-name">Steve_Notch99</div>
          <div class="user-status">
            <span class="status-dot"></span>
            Online
          </div>
        </div>
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="rgba(240,253,244,0.3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
      </div>
    </div>
  </aside>

  <!-- ─── MAIN ─── -->
  <main class="main">
    <!-- TOPBAR -->
    <div class="topbar">
      <div>
        <div class="topbar-title">Good evening ✦</div>
        <div class="topbar-sub">Ready to mine some blocks?</div>
      </div>
      <div class="topbar-right">
        <div class="search-box">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="rgba(240,253,244,0.3)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
          <input type="text" placeholder="Search versions, mods…">
        </div>
        <div class="btn-icon">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></svg>
        </div>
        <div class="btn-icon">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="23 4 23 10 17 10"/><polyline points="1 20 1 14 7 14"/><path d="M3.51 9a9 9 0 0 1 14.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0 0 20.49 15"/></svg>
        </div>
      </div>
    </div>

    <!-- SCROLLABLE CONTENT -->
    <div class="content">

      <!-- HERO -->
      <div class="hero-card">
        <div class="hero-bg"></div>
        <div class="pixel-scene">
          <div class="block-grid">
            <!-- Row 1 -->
            <div class="blk blk-x"></div><div class="blk blk-x"></div><div class="blk blk-x"></div><div class="blk blk-x"></div><div class="blk blk-x"></div><div class="blk blk-e"></div><div class="blk blk-x"></div><div class="blk blk-x"></div>
            <!-- Row 2 -->
            <div class="blk blk-x"></div><div class="blk blk-x"></div><div class="blk blk-x"></div><div class="blk blk-w"></div><div class="blk blk-w"></div><div class="blk blk-x"></div><div class="blk blk-x"></div><div class="blk blk-x"></div>
            <!-- Row 3 -->
            <div class="blk blk-x"></div><div class="blk blk-g"></div><div class="blk blk-g"></div><div class="blk blk-g"></div><div class="blk blk-g"></div><div class="blk blk-g"></div><div class="blk blk-g"></div><div class="blk blk-x"></div>
            <!-- Row 4 -->
            <div class="blk blk-d"></div><div class="blk blk-d"></div><div class="blk blk-d"></div><div class="blk blk-d"></div><div class="blk blk-d"></div><div class="blk blk-d"></div><div class="blk blk-d"></div><div class="blk blk-d"></div>
            <!-- Row 5 -->
            <div class="blk blk-s"></div><div class="blk blk-s"></div><div class="blk blk-s"></div><div class="blk blk-s"></div><div class="blk blk-s"></div><div class="blk blk-s"></div><div class="blk blk-s"></div><div class="blk blk-s"></div>
          </div>
        </div>
        <div class="hero-content">
          <div class="hero-badge"><span class="badge-dot"></span> Java Edition Active</div>
          <div>
            <div class="hero-title">Minecraft<br><span>1.21.4</span></div>
          </div>
          <div class="hero-bottom">
            <button class="btn-launch" onclick="launchGame()">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg>
              PLAY NOW
            </button>
            <div class="hero-meta">
              <div class="meta-item">
                <div class="meta-label">Last Played</div>
                <div class="meta-val">2h ago</div>
              </div>
              <div class="meta-item">
                <div class="meta-label">World</div>
                <div class="meta-val">Skyblock_v3</div>
              </div>
              <div class="meta-item">
                <div class="meta-label">Mods</div>
                <div class="meta-val">0 active</div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- VERSIONS -->
      <div>
        <div class="section-header">
          <div class="section-title">Versions</div>
          <div class="section-link">
            Manage all
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
          </div>
        </div>
        <div class="versions-grid">
          <div class="version-card selected" onclick="selectVersion(this)">
            <div class="version-icon vi-vanilla">🌿</div>
            <div class="version-name">Vanilla</div>
            <div class="version-num">1.21.4</div>
            <div class="version-tag tag-release">Release</div>
          </div>
          <div class="version-card" onclick="selectVersion(this)">
            <div class="version-icon vi-fabric">🧵</div>
            <div class="version-name">Fabric</div>
            <div class="version-num">1.21.1</div>
            <div class="version-tag tag-modded">Modded</div>
          </div>
          <div class="version-card" onclick="selectVersion(this)">
            <div class="version-icon vi-forge">⚒️</div>
            <div class="version-name">Forge</div>
            <div class="version-num">1.20.1</div>
            <div class="version-tag tag-modded">Modded</div>
          </div>
          <div class="version-card" onclick="selectVersion(this)">
            <div class="version-icon vi-paper">📄</div>
            <div class="version-name">Snapshot</div>
            <div class="version-num">25w04a</div>
            <div class="version-tag tag-snapshot">Snapshot</div>
          </div>
          <div class="version-card" onclick="selectVersion(this)">
            <div class="version-icon vi-custom">✨</div>
            <div class="version-name">Custom</div>
            <div class="version-num">My Pack</div>
            <div class="version-tag tag-custom">Custom</div>
          </div>
        </div>
      </div>

      <!-- BOTTOM ROW -->
      <div class="bottom-row">
        <!-- NEWS -->
        <div>
          <div class="section-header">
            <div class="section-title">Latest News</div>
            <div class="section-link">
              View all
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
            </div>
          </div>
          <div class="news-list">
            <div class="news-item">
              <div class="news-thumb nt-1">🌲</div>
              <div class="news-content">
                <div class="news-title">1.21.4 "Garden Awakens" — Full Changelog</div>
                <div class="news-date">April 24, 2026</div>
              </div>
              <div class="news-arrow">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
              </div>
            </div>
            <div class="news-item">
              <div class="news-thumb nt-2">🔮</div>
              <div class="news-content">
                <div class="news-title">Snapshot 25w04a — New End Biomes Preview</div>
                <div class="news-date">April 19, 2026</div>
              </div>
              <div class="news-arrow">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
              </div>
            </div>
            <div class="news-item">
              <div class="news-thumb nt-3">🔥</div>
              <div class="news-content">
                <div class="news-title">Nether Update Anniversary — Free Cape for All</div>
                <div class="news-date">April 12, 2026</div>
              </div>
              <div class="news-arrow">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
              </div>
            </div>
          </div>
        </div>

        <!-- STATS -->
        <div>
          <div class="section-header">
            <div class="section-title">Your Stats</div>
          </div>
          <div class="stats-panel">
            <div class="playtime-display">
              <div class="playtime-num">847h</div>
              <div class="playtime-label">Total Playtime</div>
            </div>
            <div class="stat-divider"></div>
            <div class="stat-row">
              <div class="stat-header">
                <span class="stat-name">RAM Usage</span>
                <span class="stat-value" style="color:var(--green)">4 GB / 16 GB</span>
              </div>
              <div class="stat-bar"><div class="stat-fill sf-green" style="width:25%"></div></div>
            </div>
            <div class="stat-row">
              <div class="stat-header">
                <span class="stat-name">Storage</span>
                <span class="stat-value" style="color:var(--sky)">12.3 GB</span>
              </div>
              <div class="stat-bar"><div class="stat-fill sf-sky" style="width:60%"></div></div>
            </div>
            <div class="stat-row">
              <div class="stat-header">
                <span class="stat-name">Mods Installed</span>
                <span class="stat-value" style="color:var(--amber)">34</span>
              </div>
              <div class="stat-bar"><div class="stat-fill sf-amber" style="width:40%"></div></div>
            </div>
          </div>
        </div>
      </div>

    </div><!-- /content -->

    <!-- LAUNCH BAR -->
    <div class="launch-bar">
      <div class="launch-version-select">
        <span class="lvs-icon">🌿</span>
        <div class="lvs-text">
          <span class="lvs-name">Vanilla 1.21.4</span>
          <span class="lvs-num">Java Edition</span>
        </div>
        <svg class="lvs-arrow" width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
      </div>

      <div class="launch-spacer"></div>

      <div class="ram-control">
        <div class="ram-label">
          <span class="ram-val" id="ramDisplay">4 GB</span>
          <span class="ram-sub">RAM Allocated</span>
        </div>
        <input type="range" class="ram-slider" min="2" max="16" value="4" step="1"
          oninput="document.getElementById('ramDisplay').textContent = this.value + ' GB'">
      </div>

      <button class="btn-launch-main" onclick="launchGame()">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg>
        LAUNCH GAME
      </button>
    </div>

  </main>
</div>

<!-- LAUNCH OVERLAY -->
<div class="launch-progress" id="launchOverlay">
  <div class="progress-modal">
    <div class="progress-icon">⛏️</div>
    <div class="progress-title">Launching Minecraft</div>
    <div class="progress-sub" id="progressSub">Loading assets…</div>
    <div class="progress-track">
      <div class="progress-fill" id="progressFill"></div>
    </div>
    <div class="progress-pct" id="progressPct">0%</div>
  </div>
</div>

<script>
  function setNav(el) {
    document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
    el.classList.add('active');
  }

  function selectVersion(el) {
    document.querySelectorAll('.version-card').forEach(c => c.classList.remove('selected'));
    el.classList.add('selected');
  }

  function launchGame() {
    const overlay = document.getElementById('launchOverlay');
    const fill    = document.getElementById('progressFill');
    const sub     = document.getElementById('progressSub');
    const pct     = document.getElementById('progressPct');

    const steps = [
      'Authenticating account…',
      'Downloading assets…',
      'Verifying game files…',
      'Loading Java runtime…',
      'Starting Minecraft…',
    ];

    overlay.classList.add('visible');
    let progress = 0;
    let step = 0;

    const interval = setInterval(() => {
      progress += Math.random() * 8 + 4;
      if (progress > 100) progress = 100;

      fill.style.width = progress + '%';
      pct.textContent = Math.floor(progress) + '%';

      const newStep = Math.floor((progress / 100) * steps.length);
      if (newStep !== step && newStep < steps.length) {
        step = newStep;
        sub.textContent = steps[step];
      }

      if (progress >= 100) {
        clearInterval(interval);
        setTimeout(() => {
          overlay.classList.remove('visible');
          fill.style.width = '0%';
          progress = 0;
          step = 0;
          sub.textContent = 'Loading assets…';
          pct.textContent = '0%';
        }, 800);
      }
    }, 120);
  }

  // Close overlay on click
  document.getElementById('launchOverlay').addEventListener('click', function(e) {
    if (e.target === this) this.classList.remove('visible');
  });
</script>

</body>
</html>
